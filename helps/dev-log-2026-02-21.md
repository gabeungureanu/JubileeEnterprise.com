# Development Log - February 21, 2026

## Summary

**Session 1 (Morning):** Nine workstreams completed: (1) Contact creation validation fixes — URL validation accepts URLs without protocol prefix, all contact fields made optional; (2) Restore from Deleted folder fix — ownership check now includes soft-deleted contacts, validation simplified; (3) Authentication hardening across WPF and Mobile — removed fake test user ID (`00000000-0000-0000-0000-000000000001`) fallback from all WPF services, added `RequireUserId()` guard in WPF and `requireUserId()` in mobile; (4) Calendar recurrence support — pulled and integrated recurring event features with migration 0007; (5) Calendar 30-Second Auto-Refresh Sync — web and mobile calendar pages now poll the Continuum API every 30 seconds with cache-busting, visibility/foreground listeners, and setTimeout chain pattern; (6) UserId Data Fragmentation Fix — migrated orphaned events, calendars, email accounts, and contacts from synthetic SHA-256-based userIds to real Codex userIds; (7) Calendar Event Creation Fix — applied migration 0007 to add missing recurrence columns, unblocking all event creation which had been failing with `column "recurrence_type" does not exist`; (8) WPF Hamburger Menu Fix — fixed folder pane toggle (replaced broken DoubleAnimation with direct GridLength setting, removed double-fire from dual Command+Click paths, fixed WindowChrome cursor override); (9) Calendar Event Attendee Autocomplete — added contact suggestions popup to the Invite Attendees field with local filtering, keyboard/mouse support, and dark-themed UI.

**Session 2 (Afternoon):** Eight workstreams completed: (1) Mobile auth alignment with web frontend — loading gate, sync-only persistence, SHA-256 userId, auto-save email on register, button loading text, keyboard handling, autoFocus, "Sign In" link on SyncEmailScreen; (2) Recurring event delete — RecurrenceActionDialog with "This event"/"All events in series" choice matching web; (3) Delete confirmation popups for all event deletions; (4) Contact creation fixes — payload format alignment (snake_case DTO), toCamelCaseKeys in service, phone validation removal; (5) Website URL validation — auto-prepends https:// matching web; (6) Date picker for birthday/anniversary — MonthGrid + BottomSheet replacing free-text inputs; (7) AttachmentPreviewModal — in-app file preview with download/share; (8) Calendar component improvements — AttendeeInput with email validation, AttachmentPicker overhaul, calendar utility enhancements.

---

## Codex API: Contact Validation Fixes

### 1. URL Validation — Accept URLs Without Protocol

Fixed `isValidUrl` to auto-prepend `https://` when no protocol is present, so URLs like `www.logixshaper.com` are accepted.

**Before:** `new URL(url)` threw on URLs without `http://` or `https://` prefix.
**After:** If no protocol detected, prepends `https://` before validation.

**File:** `services/codex-api/src/index.ts`

---

### 2. All Contact Fields Made Optional

Removed phone number requirement from `validateContactInput`. Contact creation now only performs format checks (email, URL, date formats) without requiring any specific fields.

**File:** `services/codex-api/src/index.ts`

---

## Codex API: Restore from Deleted Folder Fix

### 3. Ownership Check Includes Deleted Contacts

Fixed "Failed to restore contact" error when restoring from Deleted folder.

**Root Causes:**
- `verifyContactOwnership` excluded soft-deleted contacts (didn't pass `includeDeleted=true`)
- Validation skip condition `if (!body.isDeleted)` was incorrect for restore operations where `isDeleted=false`

**Fixes:**
- Changed PUT endpoint ownership check to `verifyContactOwnership(id, userId, true)`
- Simplified validation: always run, all fields optional — only format checks

**File:** `services/codex-api/src/index.ts`, `packages/database/src/codex.ts`

---

## WPF Application: Authentication Hardening

### 4. Remove Fake Test User ID Fallback

Removed all 14 occurrences of the hardcoded test user ID `00000000-0000-0000-0000-000000000001` across 5 WPF files. Added `RequireUserId()` method that throws `InvalidOperationException` with a clear message if the user is not authenticated.

**Pattern (before):**
```csharp
var userId = ServiceConfiguration.UserId ?? "00000000-0000-0000-0000-000000000001";
```

**Pattern (after):**
```csharp
var userId = ServiceConfiguration.RequireUserId();
// Throws: "User is not authenticated. Please sign in to continue."
```

**Files:**
| File | Occurrences |
|------|-------------|
| `Services/ServiceConfiguration.cs` | Added `RequireUserId()` method, removed default from `Initialize()` |
| `App.xaml.cs` | Removed default user ID assignment |
| `Services/ApiCalendarService.cs` | 2 replacements |
| `Services/ApiContactService.cs` | 8 replacements |
| `ViewModels/PeopleViewModel.cs` | 1 replacement |

---

## Mobile App: Authentication Hardening

### 5. Add `requireUserId()` Guard to Token Store

Added `requireUserId()` method to `tokenStore` in the mobile API client. Throws `"User is not authenticated. Please sign in to continue."` if `cachedUserId` is null.

**File:** `mobile/JubileeOutlookMobile/src/services/apiClient.ts`

---

### 6. Calendar Service — Require Authenticated UserId

Updated `getEvents()` and `createEvent()` to use `tokenStore.requireUserId()` instead of `tokenStore.getUserId()`.

**File:** `mobile/JubileeOutlookMobile/src/services/calendar/calendarService.ts` (2 calls updated)

---

### 7. Contact Service — Require Authenticated UserId

Updated all 9 methods that use userId to use `tokenStore.requireUserId()`:
`getContacts`, `createContact`, `getGroups`, `createGroup`, `updateGroup`, `deleteGroup`, `addMembersToGroup`, `removeMemberFromGroup`, `getGroupMembers`

**File:** `mobile/JubileeOutlookMobile/src/services/contacts/contactService.ts` (9 calls updated)

---

### 8. Mail Service — Require Authenticated UserId

Updated all 4 methods that use userId to use `tokenStore.requireUserId()`:
`getFolders`, `createFolder`, `searchMessages`, `getAccounts`

Also removed silent `return []` fallbacks that masked authentication failures in `getFolders` and `getAccounts`.

**File:** `mobile/JubileeOutlookMobile/src/services/mail/mailService.ts` (4 calls updated)

---

### 9. Files Not Changed (Intentionally)

The following files use `tokenStore.getUserId()` and were intentionally left unchanged:

| File | Reason |
|------|--------|
| `context/AuthContext.tsx` | Auth management — needs to check auth state without throwing |
| `context/MailContext.tsx` | Has own `getUserId` wrapper for mail operations |
| `services/apiClient.ts` (interceptor) | `attachAuth` uses soft check — gracefully omits header if no userId |
| `screens/people/ContactEditScreen.tsx` | Already has explicit null check with user-friendly `alert()` |
| `screens/calendar/NewEventScreen.tsx` | Already has explicit null check with user-friendly `alert()` |
| `screens/mail/MessageDetailScreen.tsx` | Used for attachment download headers — soft check appropriate |
| `screens/settings/SettingsScreen.tsx` | Graceful fallback acceptable for settings loading |
| `screens/auth/SyncPasswordScreen.tsx` | Auth flow — userId may not exist yet |

---

## Calendar: Recurrence Support Integration

### 10. Migration 0007 — Calendar Recurrence Fields

Ran migration `0007_calendar_recurrence_fields.sql` adding recurrence support columns to `outlook_calendar_events` table in the Continuum database.

**File:** `infrastructure/migrations/continuum/0007_calendar_recurrence_fields.sql`

---

## Web Frontend: Auth Audit (No Changes Needed)

Audited the web frontend for the same pattern. Results:
- No hardcoded test user IDs found
- Service files use `tokenStore.getUserId()` in authenticated contexts (post-login)
- Critical paths (compose, settings) have explicit null guards
- API interceptor has conditional header attachment
- **Risk level: LOW** — no changes required

---

## Files Summary

### API — Modified (2 files)
| File | Description |
|------|-------------|
| `services/codex-api/src/index.ts` | URL validation fix, optional fields, restore ownership check |
| `packages/database/src/codex.ts` | includeDeleted support for updateContact |

### WPF — Modified (5 files)
| File | Description |
|------|-------------|
| `Services/ServiceConfiguration.cs` | Added `RequireUserId()`, removed default test ID |
| `App.xaml.cs` | Removed default user ID assignment |
| `Services/ApiCalendarService.cs` | 2x `RequireUserId()` replacement |
| `Services/ApiContactService.cs` | 8x `RequireUserId()` replacement |
| `ViewModels/PeopleViewModel.cs` | 1x `RequireUserId()` replacement |

### Mobile — Modified (4 files)
| File | Description |
|------|-------------|
| `src/services/apiClient.ts` | Added `requireUserId()` method to tokenStore |
| `src/services/calendar/calendarService.ts` | 2x `requireUserId()` replacement |
| `src/services/contacts/contactService.ts` | 9x `requireUserId()` replacement |
| `src/services/mail/mailService.ts` | 4x `requireUserId()` replacement, removed silent fallbacks |

### Infrastructure — Added (1 file)
| File | Description |
|------|-------------|
| `infrastructure/migrations/continuum/0007_calendar_recurrence_fields.sql` | Calendar recurrence columns |

---

## Technical Notes

- **Auth guard pattern**: `RequireUserId()` (WPF/C#) and `requireUserId()` (mobile/TS) both throw with the same message: "User is not authenticated. Please sign in to continue." This ensures API calls never silently use a null or fake user ID.
- **Dual method pattern**: `getUserId()` remains available for soft checks (auth state inspection, interceptors, UI guards), while `requireUserId()` is used for all service-layer API calls that must have a valid user.
- **Web frontend**: Already safe — no hardcoded test IDs, reasonable null guards in critical paths.
- **Contact validation**: All fields optional at API level. Frontend forms may enforce their own requirements (e.g., at least one phone number on mobile edit screen).
- **Restore flow**: PUT endpoint with `isDeleted: false` + ownership check with `includeDeleted: true` enables proper soft-delete restore.

---

## Calendar: 30-Second Auto-Refresh Sync (Web + Mobile)

### 11. Web CalendarPage — Auto-Sync with setTimeout Chain

Added 30-second background polling to keep calendar events in sync with the database.

**Changes:**
- Reduced cache TTL from 5 minutes to 30 seconds (`CACHE_TTL_MS = 30 * 1000`)
- Added `SYNC_INTERVAL_MS = 30_000` constant
- Implemented setTimeout chain polling pattern (prevents overlapping fetches vs setInterval)
- Added `visibilitychange` listener to refresh when user returns to tab
- Added `useRef` for `syncTimerRef` and `mountedRef` to manage cleanup

**Pattern:**
```typescript
const scheduleNext = () => {
  syncTimerRef.current = setTimeout(async () => {
    await fetchEvents(true);
    if (mountedRef.current) scheduleNext();
  }, SYNC_INTERVAL_MS);
};
```

**File:** `websites/inspire/jubileeoutlook.com/jubileeoutlook-frontend/src/pages/Calendar/CalendarPage.tsx`

---

### 12. Web CalendarService — Cache Busting

Added `_t: Date.now()` query parameter to bypass CDN/browser caching on event fetches.

**File:** `websites/inspire/jubileeoutlook.com/jubileeoutlook-frontend/src/services/calendar/calendarService.ts`

---

### 13. Mobile CalendarScreen — Auto-Sync with AppState Listener

Same setTimeout chain pattern as web, plus `AppState` listener for foreground detection.

**Changes:**
- Added `SYNC_INTERVAL_MS = 30_000`
- setTimeout chain polling with `mountedRef` cleanup
- `AppState.addEventListener('change')` triggers refresh when app returns to foreground

**File:** `mobile/JubileeOutlookMobile/src/screens/calendar/CalendarScreen.tsx`

---

### 14. Mobile CalendarService — Cache Busting

Added `_t: Date.now()` cache-busting parameter matching web implementation.

**File:** `mobile/JubileeOutlookMobile/src/services/calendar/calendarService.ts`

---

## Data Layer: UserId Fragmentation Fix

### 15. Root Cause Analysis — Data Mismatch Across Platforms

Discovered data fragmentation across three different userIds for the same user (`logixasp@gmail.com`):

| UserId | Source | Platform |
|--------|--------|----------|
| `f66610b1-853e-41bd-a0b1-3d6670a68f36` | Real Codex userId (correct) | Desktop WPF |
| `187f1569-e0f3-49bc-8961-d62ea54f4985` | Synthetic SHA-256 hash of email | Web (email sync flow) |
| `00000000-0000-0000-0000-000000000001` | Default fallback | Desktop WPF (before auth) |

**Root Cause:** The web email sync flow in `SignIn.tsx` generated a synthetic UUID from `SHA-256(email)` instead of looking up the real Codex userId. All data created during sync-only mode was stored under a non-existent synthetic userId.

**Investigation:** Web showed "Test event" while desktop showed different data. Traced through all auth flows across 3 platforms (web, desktop WPF, mobile) to identify the fragmentation.

### 16. Data Migration

Migrated all orphaned data from synthetic userId to real Codex userId:

| Table | Records Migrated | Database |
|-------|-----------------|----------|
| `outlook_calendar_events` | 3 events | Continuum |
| `outlook_calendars` | 1 calendar | Continuum |
| `outlook_email_accounts` | 1 deleted (duplicate unique constraint) | Continuum |
| `user_contacts` | 1 contact | Codex |

---

### 17. Desktop WPF Auth Verification

Verified both WPF authentication paths correctly resolve the real Codex userId:

- **Email/Password:** `AuthenticationManager.cs` → `POST /api/auth/login` → gets real userId from response
- **OAuth:** `OAuth2AuthenticationService.cs` → `POST /api/auth/oauth-register` → Codex looks up user by email, returns real userId
- **ServiceConfiguration:** `SetAuthenticatedUser()` properly fires `UserChanged` event, clearing CalendarViewModel cache

**Result:** No code changes needed for WPF desktop — auth flow is correct.

---

## Database: Calendar Event Creation Fix (Critical)

### 18. Migration 0007 Applied — Unblocked Event Creation

**Problem:** The `POST /api/v1/outlook/events` endpoint was failing with:
```
column "recurrence_type" of relation "outlook_calendar_events" does not exist
```

This blocked ALL event creation across all platforms (web, desktop, mobile).

**Root Cause:** Migration `0007_calendar_recurrence_fields.sql` existed in the repo but had never been applied to the database. The Continuum API's create and update event endpoints referenced columns that didn't exist.

**Fix:** Applied migration 0007 to the Continuum database, adding 6 columns + 1 index:

| Column | Type | Purpose |
|--------|------|---------|
| `recurrence_type` | VARCHAR(20) | daily, weekly, monthly, yearly |
| `recurrence_interval` | INTEGER (default 1) | Every N days/weeks/months |
| `recurrence_end_date` | TIMESTAMPTZ | End date for recurrence |
| `recurrence_occurrences` | INTEGER | Max number of occurrences |
| `recurrence_days_of_week` | TEXT[] | Array of day names for weekly |
| `recurrence_exception_dates` | TEXT[] | Excluded dates from series |

**Index:** `idx_outlook_events_is_recurring` on `(user_id, is_recurring) WHERE is_recurring = TRUE`

**Verification:** Successfully created and deleted a test event via `POST /api/v1/outlook/events` after migration.

**File:** `infrastructure/migrations/continuum/0007_calendar_recurrence_fields.sql`

---

## Additional Files Modified (Calendar Sync + Data Fix)

### Web Frontend — Modified (2 files)
| File | Description |
|------|-------------|
| `src/pages/Calendar/CalendarPage.tsx` | 30s auto-refresh, reduced cache TTL, visibility listener |
| `src/services/calendar/calendarService.ts` | Cache-busting `_t` parameter |

### Mobile — Modified (2 files)
| File | Description |
|------|-------------|
| `src/screens/calendar/CalendarScreen.tsx` | 30s auto-refresh, AppState foreground listener |
| `src/services/calendar/calendarService.ts` | Cache-busting `_t` parameter |

### Database — Applied (1 migration)
| File | Description |
|------|-------------|
| `infrastructure/migrations/continuum/0007_calendar_recurrence_fields.sql` | 6 recurrence columns + index (applied to DB) |

---

## WPF Desktop: Hamburger Menu Folder Pane Toggle Fix

### 19. Fix Folder Pane Toggle — Replace Broken DoubleAnimation

**Problem:** Clicking the hamburger menu icon did nothing — the folder pane didn't collapse or expand.

**Root Causes (2 issues):**
1. **DoubleAnimation on GridLength**: `ColumnDefinition.WidthProperty` is `GridLength`, not `double`. The `DoubleAnimation` on `FolderPaneColumn` silently failed because WPF can't animate `GridLength` with `DoubleAnimation`.
2. **MinWidth="150"**: Even if the width was set to 0, `MinWidth="150"` on `FolderPaneColumn` prevented collapse below 150px.

**Fix:** Replaced broken animation with direct property setting:
- Set `FolderPaneColumn.MinWidth = 0` and `Width = new GridLength(0)` for collapse
- Set `FolderPaneBorder.Visibility = Collapsed` and `FolderPaneSplitter.Visibility = Collapsed`
- Restore to saved width on expand
- Removed unused `AnimateGridSplitter()` method

**File:** `applications/JubileeOutlook.wpf/JubileeOutlook/MainWindow.xaml.cs`

---

### 20. Fix Double-Fire Toggle Cancel — Remove Duplicate Click Handler

**Problem:** After fix #19, the toggle still appeared to do nothing.

**Root Cause:** The `HamburgerButton` had **both** `Command="{Binding ToggleFolderPaneCommand}"` AND `Click="HamburgerButton_Click"`. Both fired on every click, so `_isFolderPaneCollapsed` toggled **twice** per click (collapse → immediately uncollapse), canceling each other out.

**Fix:**
- Removed `Click="HamburgerButton_Click"` from `AppRailControl.xaml`
- Removed unused `HamburgerButton_Click` handler and `HamburgerMenuClicked` event from `AppRailControl.xaml.cs`
- Removed redundant backup subscription from `MainWindow.xaml.cs`

**Files:**
| File | Changes |
|------|---------|
| `Controls/AppRailControl.xaml` | Removed `Click="HamburgerButton_Click"` |
| `Controls/AppRailControl.xaml.cs` | Removed `HamburgerMenuClicked` event and `HamburgerButton_Click` handler |
| `MainWindow.xaml.cs` | Removed `AppRail.HamburgerMenuClicked` subscription |

---

### 21. Fix Hand Cursor on Hamburger Menu — WindowChrome Override

**Problem:** The hamburger menu icon didn't show a hand/pointer cursor on hover, despite `Cursor="Hand"` in the style.

**Root Cause:** The window uses `WindowChrome` with `CaptionHeight="32"`, creating a 32px non-client title bar zone at the top. The hamburger button sits in this zone, so WindowChrome overrides the cursor to the default arrow (for window dragging). Other title bar elements already had `WindowChrome.IsHitTestVisibleInChrome="True"` but the `AppRailControl` was missing it.

**Fix:**
- Added `WindowChrome.IsHitTestVisibleInChrome="True"` to `AppRailControl` in `MainWindow.xaml`
- Added `ForceCursor="True"` and `Cursor="Hand"` on Border in `AppRailMenuButtonStyle` (DarkTheme.xaml)
- Applied same fix to `AppRailButtonStyle` (Mail, Calendar, People radio buttons)

**Files:**
| File | Changes |
|------|---------|
| `MainWindow.xaml` | Added `WindowChrome.IsHitTestVisibleInChrome="True"` to AppRailControl |
| `Themes/DarkTheme.xaml` | Added `ForceCursor="True"` + `Cursor="Hand"` on Border for both AppRail styles |

---

## WPF Desktop: Calendar Event Attendee Autocomplete

### 22. Contact Suggestions Popup for Invite Attendees

**Feature:** When typing a name or email in the "Invite attendees" field of the New/Edit Event dialog, a dropdown appears showing matching contacts from the user's address book.

**Implementation:**

| Component | Description |
|-----------|-------------|
| **Local Filtering** | Loads all contacts once from `ApiContactService.Instance` on first keystroke, then filters in-memory (no API call per keystroke) |
| **Token-Based Search** | Only searches on text after the last separator (`;` or `,`), so existing attendees aren't re-searched |
| **Match Fields** | DisplayName, FirstName, LastName, EmailAddresses |
| **Exclusion** | Already-added attendees are excluded from suggestions |
| **Max Results** | 8 contacts shown at a time |
| **Keyboard Support** | Enter selects first suggestion, Escape closes popup, Down arrow focuses list |
| **Mouse Support** | Click on a suggestion to select it |
| **Display** | Shows DisplayName in the attendees field (not email) |

**Popup UI:** Dark-themed dropdown matching the app's design — each suggestion shows initials circle + display name + email address.

**Files:**
| File | Changes |
|------|---------|
| `ViewModels/NewEventViewModel.cs` | Added `SuggestedContacts`, `ShowContactSuggestions`, `FilterContactsAsync()`, `SelectContact()`, injected `ApiContactService` |
| `Views/NewEventWindow.xaml` | Added `Popup` with styled `ListBox` below attendees TextBox, `x:Name` and event handlers on TextBox |
| `Views/NewEventWindow.xaml.cs` | Added `TextChanged`, `PreviewKeyDown`, `ContactSuggestion_Selected`, `ApplyContactSelection` handlers |

---

## Complete Files Summary (Session Additions)

### WPF — Modified (7 files)
| File | Description |
|------|-------------|
| `Controls/AppRailControl.xaml` | Removed redundant `Click` handler from HamburgerButton |
| `Controls/AppRailControl.xaml.cs` | Removed `HamburgerMenuClicked` event and handler (cleanup) |
| `MainWindow.xaml` | Added `WindowChrome.IsHitTestVisibleInChrome` to AppRailControl |
| `MainWindow.xaml.cs` | Fixed folder pane toggle (direct GridLength/Visibility), removed duplicate subscription |
| `Themes/DarkTheme.xaml` | Added `ForceCursor`/`Cursor` fixes to AppRail button styles |
| `ViewModels/NewEventViewModel.cs` | Added attendee autocomplete: contact filtering + selection logic |
| `Views/NewEventWindow.xaml` | Added contact suggestions Popup with styled ListBox |
| `Views/NewEventWindow.xaml.cs` | Added TextChanged/KeyDown/click handlers for autocomplete |

---

## Additional Technical Notes

- **setTimeout chain vs setInterval**: setTimeout chain prevents overlapping fetches — the next poll only starts after the previous one completes. Safer than setInterval which fires regardless of callback completion.
- **Cache-busting `_t` parameter**: `Date.now()` timestamp appended as query parameter forces the API to bypass any intermediate caching (CDN, browser, proxy).
- **Visibility/AppState listeners**: Ensure data is fresh when user returns to the app after switching tabs (web) or switching apps (mobile).
- **Migration 0007 idempotency**: Uses `ADD COLUMN IF NOT EXISTS` and `CREATE INDEX IF NOT EXISTS` — safe to run multiple times.
- **Table schema dual recurrence storage**: `recurrence_rule` (TEXT, from 0003) coexists with the 6 new dedicated columns (from 0007). The API uses the dedicated columns; `recurrence_rule` remains for potential future RFC 5545 RRULE string storage.
- **GridLength vs DoubleAnimation**: WPF `ColumnDefinition.Width` is `GridLength`, not `double`. `DoubleAnimation` silently fails — use direct property assignment instead.
- **WindowChrome cursor override**: Elements in the `CaptionHeight` zone of `WindowChrome` have their cursor overridden. Use `WindowChrome.IsHitTestVisibleInChrome="True"` to restore normal cursor behavior.
- **Dual event paths (Command + Click)**: When a WPF Button has both `Command` binding and `Click` handler, both fire on click. If both paths lead to the same method, it executes twice — use only one path.
- **Contact type namespace conflict**: `JubileeOutlook.ViewModels` namespace contains a `Contact` class (PeopleViewModel.cs) that shadows `JubileeOutlook.Models.Contact`. Use `Models.Contact` explicitly in ViewModel code.

---

## Contact Duplicate Validation (Display Name + Phone) — All Platforms

### 26. Database: Rewrite Duplicate Detection

Rewrote `findDuplicateContacts` in `packages/database/src/codex.ts` to match by **Display Name AND Phone Number** (both must match). Checks both active and soft-deleted contacts, normalizes phone digits.

**File:** `packages/database/src/codex.ts`

### 27. Codex API: Duplicate Response Codes

Updated `POST /contacts` to return distinct responses:
- `DUPLICATE_DETECTED` for active duplicates (409)
- `DELETED_DUPLICATE_FOUND` for soft-deleted matches (409) — allows client to offer restore

**File:** `services/codex-api/src/index.ts`

### 28. Cross-Platform Duplicate Handling

- **Web:** Frontend duplicate check before save, import duplicate detection with soft-deleted restore, restore flow warns on active duplicates, batch operations skip duplicates, bulk delete/restore fixes, import format fix (JSON vs FormData), batch field name fix (`ids` vs `contactIds`)
- **WPF:** Handle `DELETED_DUPLICATE_FOUND` with restore dialog, added `RestoreContactAsync` via PATCH endpoint
- **Mobile:** Handle 409 duplicate responses in `createContact`, offer restore for deleted duplicates

**Files:** `PeoplePage.tsx`, `contactService.ts` (web), `ContactDialog.tsx`, `PeopleRibbon.tsx`, `ApiContactService.cs`, `PeopleViewModel.cs`, `ContactEditScreen.tsx` (mobile)

---

## Web Frontend: Jubilee Branding

### 29. Favicon & PWA Icons Replaced

Replaced default Create React App favicon and icons with Jubilee AI character branding:
- `favicon.ico` — Jubilee AI character .ico (184KB, from WPF JubileeBrowser)
- `logo192.png` — Jubilee character resized to 192x192 (via sharp)
- `logo512.png` — Jubilee character resized to 512x512
- `jubilee-icon.svg` — Gold envelope SVG added as SVG favicon
- Updated `index.html` with SVG favicon link and theme-color `#ffbd59`
- Updated `manifest.json` with `theme_color: #ffbd59`, `background_color: #000000`

**Files:** `public/favicon.ico`, `public/logo192.png`, `public/logo512.png`, `public/jubilee-icon.svg`, `public/index.html`, `public/manifest.json`

### 30. Sign-In Page: Jubilee Logo Avatar

Replaced the Material Symbols `person` icon placeholder in the sign-in page's gold circle avatar with the Jubilee AI character image (`jubilee-logo.png`). All 3 sign-in panels updated: email sync, sync-password, and signin/signup.

**Files:** `src/pages/Auth/SignIn.tsx`, `src/pages/Auth/SignIn.css`, `src/assets/jubilee-logo.png` (new)

### 31. Sign-In Page: Remove Dropdown Arrow

Removed the misleading `arrow_drop_down` Material icon from the email text input field.

**File:** `src/pages/Auth/SignIn.tsx`

---

## Calendar: Attendee Autocomplete with Contact Search

### 32. EventDialog: RecipientInput Integration

Replaced the plain text attendees input in the EventDialog with the existing `RecipientInput` component (reused from mail compose). As the user types (2+ characters), contacts are searched via Codex API (`/contacts/search`) with 300ms debounce. Matching contacts appear in an autocomplete dropdown with avatar initials, display name, and email. Supports keyboard navigation (Arrow Up/Down, Enter, Tab, Escape).

**Changes:**
- Import `RecipientInput` and `Recipient` type from `components/mail/RecipientInput/`
- Replace `attendees: string` state with `attendeeList: Recipient[]`
- Wire RecipientInput with `chipDisplay="name"` prop
- On save, extract emails from Recipient objects for the API

**File:** `src/components/calendar/EventDialog/EventDialog.tsx`

### 33. RecipientInput: chipDisplay Prop

Added `chipDisplay?: 'full' | 'name'` prop to RecipientInput:
- `'full'` (default): Shows `Name <email>` — used in mail compose
- `'name'`: Shows just the display name — used in calendar attendees
- Full email visible on hover via tooltip

**File:** `src/components/mail/RecipientInput/RecipientInput.tsx`

### 34. EventDialog CSS: Attendees Styling

Added `.event-dialog__field--attendees` styling for RecipientInput integration: container with relative positioning, dark theme background/border, focus-within gold highlight, dropdown positioned below the field.

**File:** `src/components/calendar/EventDialog/EventDialog.css`

---
---

# Session 2 — Afternoon

---

## Mobile Auth: Alignment with Web Frontend (8 Changes)

### 19. Loading Gate — Eliminate AuthStack Flash

**Problem:** Returning authenticated users briefly see AuthStack before bootstrap completes.

**Fix:** `RootNavigator.tsx` gates on `isLoading` from AuthContext, shows `LoadingSpinner` fullScreen during bootstrap. `AuthContext.tsx` optimizes initial state — if tokens exist, starts loading; if no tokens, renders AuthStack instantly.

**Files:** `src/navigation/RootNavigator.tsx`, `src/context/AuthContext.tsx`

---

### 20. Sync-Only Persistence on App Restart

**Problem:** Sync-only users had to re-sync after every app restart (web persists automatically).

**Fix:** Enabled sync-only detection in bootstrap. Calls existing `checkSyncOnly()` → `resolveSyncUserId()` → `buildSyncUser()` helpers during app initialization.

**File:** `src/context/AuthContext.tsx`

---

### 21. SHA-256 Hash Alignment for Sync UserId

**Problem:** Web uses `crypto.subtle.digest('SHA-256')` for fallback sync userId. Mobile used a simple `charCodeAt` loop producing different IDs for the same email.

**Fix:** Replaced with `expo-crypto` SHA-256 digest matching web's UUID format (`hash.slice(0,8)-hash.slice(8,12)-4hash.slice(13,16)-...`).

**Dependency:** Added `expo-crypto` (~15.0.8)

**File:** `src/screens/auth/SyncPasswordScreen.tsx`

---

### 22. Auto-Save Email After Registration

**Fix:** `register` callback now saves email to AsyncStorage after successful registration, matching web behavior.

**File:** `src/context/AuthContext.tsx`

---

### 23. Button Loading States — Text Instead of Spinner

**Fix:** Auth buttons show loading text ("Signing in...", "Creating account...", "Sending reset link...") instead of ActivityIndicator spinner, matching web.

**Files:** `src/screens/auth/SignInScreen.tsx`, `src/screens/auth/SignUpScreen.tsx`, `src/screens/auth/ForgotPasswordScreen.tsx`

---

### 24. Keyboard Submit Handling + Input Chaining

**Fix:** All auth screen inputs have `returnKeyType` ("next"/"go") and `onSubmitEditing` handlers. Multi-input screens chain focus via `useRef<TextInput>()`.

**Files:** All 5 auth screens

---

### 25. AutoFocus on First Input

**Fix:** Added `autoFocus` to first input on `SyncEmailScreen`, `SignInScreen`, `SignUpScreen`, `ForgotPasswordScreen`.

---

### 26. "Sign In" Link on SyncEmailScreen

**Fix:** Added "Already have an account? Sign In" link below Continue button.

**File:** `src/screens/auth/SyncEmailScreen.tsx`

---

## Calendar: Recurring Event Delete

### 27. RecurrenceActionDialog (New Component)

Created `RecurrenceActionDialog.tsx` — modal dialog matching web frontend's `RecurrenceEditDialog`. Supports 'edit' and 'delete' modes with "This event" / "All events in series" / "Cancel" options.

**Type:** `RecurrenceAction = 'series' | 'occurrence' | 'cancel'`

**File:** `src/components/common/RecurrenceActionDialog.tsx` (184 lines, NEW)

---

### 28. EventDetailScreen — Recurring Delete + Confirmation

**Changes:**
- Added `getBaseEventId()` — strips `_occ_N` suffix from expanded occurrence IDs (matches web)
- `executeDelete(scope)` — calls `calendarService.deleteEvent(baseId, scope, occurrenceDate)` with correct params
- `handleDelete()` — detects `event.isRecurring`, shows RecurrenceActionDialog for recurring events
- `handleRecurrenceAction()` — after choosing scope, shows confirmation dialog before executing delete
- Non-recurring events: simple confirm → delete (unchanged UX, just refactored to use `executeDelete`)

**File:** `src/screens/calendar/EventDetailScreen.tsx`

---

## Calendar: Attachment Preview

### 29. AttachmentPreviewModal (New Component)

Created `AttachmentPreviewModal.tsx` — in-app file preview modal with:
- File name, size display
- Open in browser (via expo-web-browser)
- Download and share (via expo-file-system + expo-sharing)
- Close button

**Dependency:** Added `expo-web-browser` (~15.0.10)

**File:** `src/components/common/AttachmentPreviewModal.tsx` (341 lines, NEW)

---

### 30. EventDetailScreen — Attachment Preview Integration

Changed attachment tap from direct `Linking.openURL()` to opening `AttachmentPreviewModal` with file name, URL, and size.

**File:** `src/screens/calendar/EventDetailScreen.tsx`

---

## Contacts: Creation Fixes (4 Iterations)

### 31. Phone Validation Removal

Removed required phone number validation from ContactEditScreen. Web doesn't require phone; mobile now matches.

**File:** `src/screens/people/ContactEditScreen.tsx`

---

### 32. Payload Format Alignment (snake_case DTO)

Changed ContactEditScreen payload from camelCase `CreateContactPayload` to snake_case `Partial<ContactDto>`. The service's `toCamelCaseKeys()` converts to camelCase for the API, matching the web frontend's exact flow.

**Key change:** `birthday` and `anniversary` use `|| null` (not `|| undefined`) matching database DATE column expectations.

**Files:** `src/screens/people/ContactEditScreen.tsx`, `src/services/contacts/contactService.ts`

---

### 33. contactService — toCamelCaseKeys + extractApiError

Added two helper functions matching web frontend:
- `toCamelCaseKeys()` — converts snake_case DTO keys to camelCase before API submission
- `extractApiError()` — extracts human-readable error from Axios error responses
- Updated `createContact` to handle 409 duplicate responses with error code and deletedContactId
- Updated `updateContact` to use toCamelCaseKeys

**File:** `src/services/contacts/contactService.ts`

---

### 34. Website URL Validation

Added URL validation matching web's `ContactDialog.tsx` — auto-prepends `https://` if no protocol, validates with `new URL()`.

**File:** `src/screens/people/ContactEditScreen.tsx`

---

### 35. Date Picker for Birthday/Anniversary

Replaced free-text inputs with proper date pickers using existing `MonthGrid` + `BottomSheet` components.

**Implementation:**
- `datePickerTarget` state tracks which field is being edited ('birthday' | 'anniversary')
- `openDatePicker()` initializes browse year/month from current value or today
- `handleDateSelect()` formats selected date as YYYY-MM-DD string
- `renderDateField()` — tappable field with calendar icon, formatted date display, clear button
- BottomSheet with month navigation and MonthGrid for selection

**File:** `src/screens/people/ContactEditScreen.tsx`

---

## Calendar Components: Improvements

### 36. AttendeeInput — Email Validation + Chip UI

Enhanced `AttendeeInput.tsx` with email validation, attendee chip display, remove button, and keyboard handling.

**File:** `src/components/modules/calendar/AttendeeInput.tsx` (+151 lines)

---

### 37. AttachmentPicker — Overhaul

Refactored `AttachmentPicker.tsx` for proper Expo file API usage with base64 encoding.

**File:** `src/components/modules/calendar/AttachmentPicker.tsx` (~82 line changes)

---

### 38. NewEventScreen — Attendee + Utility Improvements

Enhanced NewEventScreen with guessMimeType utility for attachment uploads and attendee input integration.

**File:** `src/screens/calendar/NewEventScreen.tsx`

---

### 39. Calendar Types + Utils

- Updated `calendar.ts` types for recurrence/attachment changes
- Enhanced `calendarUtils.ts` with new date/time utility functions

**Files:** `src/types/calendar.ts`, `src/utils/calendarUtils.ts`

---

### 40. ReminderContext — Dismiss Actions

Added reminder dismiss/snooze action handlers to `ReminderContext.tsx`.

**File:** `src/context/ReminderContext.tsx`

---

## Session 2 Files Summary

### Mobile — New Files (2)
| File | Description |
|------|-------------|
| `src/components/common/AttachmentPreviewModal.tsx` | File preview with download/share (341 lines) |
| `src/components/common/RecurrenceActionDialog.tsx` | Recurring event action chooser (184 lines) |

### Mobile — Modified (19 files)
| File | Description |
|------|-------------|
| `src/components/common/index.ts` | Added exports for AttachmentPreviewModal, RecurrenceActionDialog |
| `src/components/modules/calendar/AttachmentPicker.tsx` | File API overhaul |
| `src/components/modules/calendar/AttendeeInput.tsx` | Email validation + chip UI |
| `src/context/AuthContext.tsx` | Loading gate, sync-only bootstrap, auto-save email |
| `src/context/ReminderContext.tsx` | Dismiss/snooze actions |
| `src/navigation/RootNavigator.tsx` | Loading gate with LoadingSpinner |
| `src/screens/auth/ForgotPasswordScreen.tsx` | Button loading text, autoFocus, keyboard |
| `src/screens/auth/SignInScreen.tsx` | Button loading text, autoFocus, keyboard, ref chaining |
| `src/screens/auth/SignUpScreen.tsx` | Button loading text, autoFocus, keyboard, ref chaining |
| `src/screens/auth/SyncEmailScreen.tsx` | "Sign In" link, autoFocus, keyboard |
| `src/screens/auth/SyncPasswordScreen.tsx` | SHA-256 via expo-crypto, keyboard |
| `src/screens/calendar/EventDetailScreen.tsx` | Recurring delete, confirmation, attachment preview |
| `src/screens/calendar/NewEventScreen.tsx` | Attendee + utility improvements |
| `src/screens/people/ContactEditScreen.tsx` | Date pickers, URL validation, snake_case DTO, phone optional |
| `src/services/contacts/contactService.ts` | toCamelCaseKeys, extractApiError, duplicate handling |
| `src/types/calendar.ts` | Type updates |
| `src/utils/calendarUtils.ts` | New utility functions |
| `package.json` | +expo-crypto, +expo-web-browser |
| `app.json` | Config updates |

### Dependencies Added
| Package | Version | Purpose |
|---------|---------|---------|
| `expo-crypto` | ~15.0.8 | SHA-256 hashing for sync userId alignment with web |
| `expo-web-browser` | ~15.0.10 | In-app browser for attachment preview |

---

## Session 2 Technical Notes

- **RecurrenceActionDialog pattern**: Matches web's `RecurrenceEditDialog` — returns `'series' | 'occurrence' | 'cancel'`. Caller decides confirmation and execution.
- **getBaseEventId**: Strips `_occ_N` suffix using regex `/_occ_\d+$/` — expanded occurrences have IDs like `uuid_occ_3`, but the API expects the base event UUID.
- **toCamelCaseKeys**: `key.replace(/_([a-z])/g, (_, ch) => ch.toUpperCase())` — converts `snake_case` to `camelCase`. Applied to contact payloads before API submission, matching web's exact pattern.
- **Date picker**: Reuses existing `MonthGrid` component (from calendar module) inside a `BottomSheet` for contact birthday/anniversary selection. Dates stored as YYYY-MM-DD strings.
- **Auth loading gate**: `isLoading` from AuthContext prevents AuthStack flash. If tokens exist on init, shows LoadingSpinner; if no tokens, renders AuthStack immediately (no delay).
- **SHA-256 alignment**: Web and mobile now produce identical fallback userIds for the same email via SHA-256. Critical for sync-only users whose data must be accessible on both platforms.
