Investigate and fix this error or issue that occurred, because your previous implementation did not work. Start by reviewing all project documentation, configuration files, and relevant source code so you understand the project end-to-end before making changes. Enable detailed logging and auditing across the full execution path, including startup, configuration loading, API calls or web requests, UI rendering, and any background processes, and ensure logs are actionable (timestamps, severity levels, correlation IDs where applicable). Reproduce the failure reliably by stepping through the workflow in a controlled manner, and use the ClaudeBrowser.exe as needed to visually validate behavior, confirm UI outcomes, and verify whether fixes resolve the issue in real conditions. Troubleshoot systematically by isolating the failing component, validating assumptions, checking dependencies, and tracing errors from symptom back to root cause, and conduct deeper research when necessary by consulting official documentation, known issues, and relevant implementation references. Track and record how many times the same problem has been requested to be fixed; if the same issue recurs more than two times, escalate immediately into comprehensive investigation mode by performing a full root-cause analysis, documenting findings, identifying systemic failures (process, architecture, or environment), and implementing a durable fix rather than a patch. Prioritize prompt bug resolution as a critical requirement, and verify the fix with repeatable tests and clear evidence from logs, audits, and visual confirmation. If the issue is design-related, fully analyze the intended design requirements, including layout placement, typography, pixel spacing, margins, padding, alignment, and the use of empty space, and validate the UI against the design standard with precise measurement rather than approximation. If you do not have a current design template or authoritative reference, request one immediately; otherwise, locate and use previous approved designs and mockups as the baseline, and align the implementation accordingly. If you require additional information at any point, ask for it explicitly and immediately, specifying exactly what is needed to proceed, and then continue stepping through the troubleshooting process until the root cause is identified and the issue is fixed and verified.