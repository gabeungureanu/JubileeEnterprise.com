<UserControl x:Class="JubileeMusic.Views.CreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource BackgroundColor}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
        <Grid MaxWidth="800" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Create New Music"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextColor}"/>
                <TextBlock Text="Enter lyrics and style to generate music with Suno AI"
                           FontSize="14"
                           Foreground="{DynamicResource TextSecondaryColor}"
                           Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Title Input -->
            <StackPanel Grid.Row="1" Margin="0,0,0,16">
                <TextBlock Text="Title (Optional)"
                           Foreground="{DynamicResource TextSecondaryColor}"
                           Margin="0,0,0,4"/>
                <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBoolConverter}}"/>
            </StackPanel>

            <!-- Lyrics Input -->
            <StackPanel Grid.Row="2" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Lyrics"
                               Foreground="{DynamicResource TextSecondaryColor}"
                               Margin="0,0,0,4"/>

                    <CheckBox Grid.Column="1"
                              Content="Instrumental Only"
                              IsChecked="{Binding IsInstrumentalOnly}"
                              Foreground="{DynamicResource TextSecondaryColor}"
                              VerticalAlignment="Center"
                              IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBoolConverter}}"/>
                </Grid>

                <TextBox Text="{Binding Lyrics, UpdateSourceTrigger=PropertyChanged}"
                         Height="200"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBoxStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsInstrumentalOnly}" Value="True">
                                    <Setter Property="Opacity" Value="0.5"/>
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsGenerating}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>

            <!-- Style Prompt -->
            <StackPanel Grid.Row="3" Margin="0,0,0,16">
                <TextBlock Text="Style / Genre"
                           Foreground="{DynamicResource TextSecondaryColor}"
                           Margin="0,0,0,4"/>
                <TextBox Text="{Binding StylePrompt, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         IsEnabled="{Binding IsGenerating, Converter={StaticResource InverseBoolConverter}}"/>
                <TextBlock Text="Examples: pop, rock ballad, acoustic folk, electronic dance, jazz piano"
                           FontSize="11"
                           Foreground="{DynamicResource TextMutedColor}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Progress Section -->
            <Border Grid.Row="4"
                    Background="{DynamicResource SurfaceColor}"
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,0,16"
                    Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <TextBlock Text="{Binding GenerationProgress}"
                               Foreground="{DynamicResource TextColor}"
                               FontWeight="Medium"/>
                    <ProgressBar Value="{Binding GenerationProgressPercent}"
                                 Maximum="100"
                                 Height="8"
                                 Margin="0,12,0,0"
                                 Foreground="{DynamicResource AccentColor}"
                                 Background="{DynamicResource BackgroundColor}"/>
                </StackPanel>
            </Border>

            <!-- Error Display -->
            <Border Grid.Row="4"
                    Background="{DynamicResource ErrorColor}"
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,0,16"
                    Opacity="0.9"
                    Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="White"
                           TextWrapping="Wrap"/>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Reset"
                        Command="{Binding ResetFormCommand}"
                        Style="{StaticResource ModernButtonStyle}"
                        Width="100" Margin="0,0,8,0"/>

                <Button Content="Cancel"
                        Command="{Binding CancelGenerationCommand}"
                        Style="{StaticResource ModernButtonStyle}"
                        Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVis}}"
                        Width="100" Margin="0,0,8,0"/>

                <Button Content="Generate"
                        Command="{Binding GenerateCommand}"
                        Style="{StaticResource AccentButtonStyle}"
                        Width="120"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
