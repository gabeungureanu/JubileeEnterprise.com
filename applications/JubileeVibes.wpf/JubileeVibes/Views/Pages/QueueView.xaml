<UserControl x:Class="JubileeVibes.Views.Pages.QueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="32,24,32,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Queue"
                       Style="{StaticResource HeadingLarge}"/>

            <Button Grid.Column="1"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Content="Clear queue"
                    Command="{Binding ClearQueueCommand}"/>
        </Grid>

        <ScrollViewer Grid.Row="1"
                      Style="{StaticResource VibesScrollViewerStyle}">
            <StackPanel Margin="32,0">

                <!-- Now Playing -->
                <StackPanel Visibility="{Binding CurrentTrack, Converter={StaticResource NullToVisibilityConverter}}"
                            Margin="0,0,0,32">
                    <TextBlock Text="Now playing"
                               Style="{StaticResource HeadingSmall}"
                               Margin="0,0,0,16"/>
                    <Border Height="64" Background="{StaticResource BackgroundTertiaryBrush}"
                            CornerRadius="4" Padding="16,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="48" Height="48"
                                    CornerRadius="4"
                                    Background="{StaticResource BackgroundHighlightBrush}">
                                <Image Source="{Binding CurrentTrack.AlbumArtUrl}" Stretch="UniformToFill"/>
                            </Border>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                                <TextBlock Text="{Binding CurrentTrack.Title}"
                                           Foreground="{StaticResource AccentPrimaryBrush}"
                                           FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding CurrentTrack.ArtistName}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="12"/>
                            </StackPanel>

                            <TextBlock Grid.Column="2"
                                       Text="{Binding CurrentTrack.DurationFormatted}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Up Next -->
                <StackPanel Margin="0,0,0,32">
                    <TextBlock Text="Next in queue"
                               Style="{StaticResource HeadingSmall}"
                               Margin="0,0,0,16"/>

                    <ItemsControl ItemsSource="{Binding Queue}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Height="56" Background="Transparent" Cursor="Hand"
                                        Margin="0,0,0,4" CornerRadius="4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftDoubleClick"
                                                      Command="{Binding DataContext.PlayItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      CommandParameter="{Binding}"/>
                                    </Border.InputBindings>
                                    <Grid Margin="16,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="48"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Width="40" Height="40"
                                                CornerRadius="4"
                                                Background="{StaticResource BackgroundTertiaryBrush}">
                                            <Image Source="{Binding Track.AlbumArtUrl}" Stretch="UniformToFill"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0">
                                            <TextBlock Text="{Binding Track.Title}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontSize="14"/>
                                            <TextBlock Text="{Binding Track.ArtistName}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="12"/>
                                        </StackPanel>

                                        <Button Grid.Column="2"
                                                Style="{StaticResource IconButtonStyle}"
                                                ToolTip="Remove">
                                            <Path Data="{StaticResource CloseIcon}"
                                                  Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                  Width="12" Height="12" Stretch="Uniform"/>
                                        </Button>

                                        <TextBlock Grid.Column="3"
                                                   Text="{Binding Track.DurationFormatted}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Empty State -->
                    <TextBlock Text="Your queue is empty"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Visibility="{Binding Queue.Count, Converter={StaticResource ZeroToVisibilityConverter}, Mode=OneWay}"
                               Margin="0,32"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
